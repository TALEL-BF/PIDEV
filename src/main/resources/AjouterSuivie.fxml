<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.chart.CategoryAxis?>
<?import javafx.scene.chart.LineChart?>
<?import javafx.scene.chart.NumberAxis?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane fx:id="root"
            xmlns="http://javafx.com/javafx/25"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="Controller.AjouterSuivie"
            stylesheets="@styles/app.css">

    <!-- =========================
         SIDEBAR (moderne, clean)
         ========================= -->
    <left>
        <VBox styleClass="sb" spacing="14" prefWidth="250">
            <HBox spacing="10" alignment="CENTER_LEFT" styleClass="sbHeader">
                <ImageView fitHeight="36" fitWidth="36" preserveRatio="true">
                    <image><Image url="@assets/logo.png"/></image>
                </ImageView>
                <VBox spacing="2">
                    <Label text="AutiCare" styleClass="sbTitle"/>
                    <Label text="Plateforme éducative" styleClass="sbSub"/>
                </VBox>
            </HBox>

            <VBox styleClass="sbUser" spacing="6">
                <Label text="Connecté en tant que" styleClass="sbMuted"/>
                <Label text="Admin" styleClass="sbBadge"/>
            </VBox>

            <VBox styleClass="sbMenu" spacing="6">
                <Button text="Gestion Utilisateur" styleClass="sbItem"/>
                <Button text="Gestion Cours &amp; Evaluations" styleClass="sbItem"/>
                <Button text="Gestion Emploi" styleClass="sbItem"/>

                <HBox spacing="8" alignment="CENTER_LEFT" styleClass="sbRow">
                    <Button fx:id="btnGestionConsultations"
                            text="Gestion Consultations"
                            styleClass="sbItem"
                            HBox.hgrow="ALWAYS"
                            maxWidth="1.7976931348623157E308"/>
                    <ToggleButton fx:id="tbConsultations" text="▸" styleClass="sbChevron"/>
                </HBox>

                <VBox fx:id="consultationsSubMenu" managed="false" visible="false" spacing="6" styleClass="sbSubMenu">
                    <Button fx:id="btnSubSuivie" text="Suivie" styleClass="sbSubItem"/>
                    <Button fx:id="btnSubTherapie" text="Thérapie" styleClass="sbSubItem"/>
                    <Button fx:id="btnSubArticles" text="Articles" styleClass="sbSubItem"/>
                </VBox>

                <Button text="Gestion Evenements" styleClass="sbItem"/>
                <Button text="Gestion Jeux" styleClass="sbItem"/>
            </VBox>

            <Region VBox.vgrow="ALWAYS"/>

            <Button text="⎋ Déconnexion" styleClass="sbLogout"/>
        </VBox>
    </left>

    <!-- =========================
         CONTENT
         ========================= -->
    <center>
        <StackPane styleClass="pageBg">

            <ScrollPane fx:id="mainScroll"
                        fitToWidth="true"
                        hbarPolicy="NEVER"
                        vbarPolicy="AS_NEEDED"
                        pannable="true"
                        styleClass="pageScroll">
                <content>
                    <VBox spacing="16" styleClass="page" maxWidth="1200">

                        <!-- Top Bar -->
                        <HBox alignment="CENTER_LEFT" spacing="12" styleClass="topbar">
                            <VBox spacing="2" HBox.hgrow="ALWAYS">
                                <Label text="Gestion des Suivies" styleClass="h1"/>
                                <Label text="Gérez les suivies et les informations associées" styleClass="sub"/>
                            </VBox>

                            <TextField fx:id="searchField" promptText="Rechercher un suivi..." styleClass="searchField"/>

                            <!-- ✅ option: si tu veux garder فقط FAB, cache هذا -->
                            <Button fx:id="btnAjouter" text="+ Ajouter" styleClass="btnPrimary"
                                    visible="false" managed="false"/>
                        </HBox>

                        <!-- Stats header (toggle) -->
                        <HBox alignment="CENTER_LEFT" spacing="10" styleClass="sectionBar">
                            <Label fx:id="lblToggleStats" text="▸ Statistiques" styleClass="sectionTitle"/>
                            <Region HBox.hgrow="ALWAYS"/>
                        </HBox>

                        <!-- Stats content -->
                        <VBox fx:id="statsBox" managed="false" visible="false" spacing="12" styleClass="statsWrap">
                            <HBox spacing="12" alignment="CENTER_LEFT" styleClass="statsTop">
                                <Label text="Suivi mensuel" styleClass="statsTitle"/>
                                <Region HBox.hgrow="ALWAYS"/>
                                <ComboBox fx:id="cbEnfantStats" promptText="Choisir un enfant..." styleClass="comboModern"/>
                                <Button fx:id="btnRefreshStats" text="Actualiser" styleClass="btnGhost"/>
                            </HBox>

                            <HBox spacing="12" styleClass="statsGrid">
                                <VBox spacing="6" styleClass="kpiCard">
                                    <Label text="Δ Humeur" styleClass="kpiK"/>
                                    <Label fx:id="lblDeltaHumeur" text="-" styleClass="kpiV"/>
                                </VBox>

                                <VBox spacing="6" styleClass="kpiCard">
                                    <Label text="Δ Stress (mieux si +)" styleClass="kpiK"/>
                                    <Label fx:id="lblDeltaStress" text="-" styleClass="kpiV"/>
                                </VBox>

                                <VBox spacing="6" styleClass="kpiCard">
                                    <Label text="Δ Attention" styleClass="kpiK"/>
                                    <Label fx:id="lblDeltaAttention" text="-" styleClass="kpiV"/>
                                </VBox>

                                <Region HBox.hgrow="ALWAYS"/>
                            </HBox>

                            <VBox styleClass="chartCard">
                                <LineChart fx:id="monthlyChart" prefHeight="260" animated="false">
                                    <xAxis><CategoryAxis label="Mois"/></xAxis>
                                    <yAxis><NumberAxis label="Moyenne"/></yAxis>
                                </LineChart>
                            </VBox>
                        </VBox>

                        <!-- Count -->
                        <HBox alignment="CENTER_LEFT">
                            <Label fx:id="countLabel" text="0 Suivies" styleClass="count"/>
                        </HBox>

                        <!-- Empty -->
                        <VBox fx:id="emptyBox" managed="false" visible="false" spacing="6" styleClass="emptyCard">
                            <Label text="Aucun suivi trouvé" styleClass="emptyTitle"/>
                            <Label text="Essayez une autre recherche ou ajoutez un suivi." styleClass="emptySub"/>
                        </VBox>

                        <!-- Cards -->
                        <VBox styleClass="cardsContainer">
                            <FlowPane fx:id="cardsPane"
                                      alignment="CENTER"
                                      hgap="20"
                                      vgap="20"
                                      prefWrapLength="1100"
                                      styleClass="cardsWrap"/>
                        </VBox>

                    </VBox>
                </content>
            </ScrollPane>

            <!-- ✅ FAB: دائما visible حتى مع scroll -->
            <Button fx:id="btnAjouterFab"
                    text="＋"
                    styleClass="fabAdd"
                    StackPane.alignment="CENTER_RIGHT">
                <StackPane.margin>
                    <Insets top="0" right="24" bottom="0" left="0"/>
                </StackPane.margin>
            </Button>

        </StackPane>
    </center>

</BorderPane>
